# Zoey Sosa, functions notes python

# functions hold actions to be reused 

def add(numOne, numTwo): # parameters set the name of the varible (just for the function)
    return numOne + numTwo

number= (int(input("please tell me a number":\n")))
print (add (number,21)) # argument set the value of the varibles just for that instance of the function
#add(8,12)
#add(6,4)
#add(11,1000) 
#add(60,40)

def values(type):

values("name")

name = value("name")
place = value("place")
verb = value("verb (past tence)")

print(f"{name} was really fast getting to {place} because they {verb} the whole way there.")